ruoyi:
    # 文件路径 示例（ Windows配置D:/ruoyi/uploadPath，Linux配置 /home/ruoyi/uploadPath）
    profile: D:/ruoyi/uploadPath
server:
    # 服务器的HTTP端口，默认为8080
    port: 8080
# 数据源配置
spring:
    datasource:
        type: com.alibaba.druid.pool.DruidDataSource
        driverClassName: com.mysql.cj.jdbc.Driver
        druid:
            # 主库数据源
            master:
                url: jdbc:mysql://106.12.15.6:3306/pi_en?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
                username: root
                password: 1qaz@WSX3edc
            # 从库数据源
            slave:
                # 从数据源开关/默认关闭
                enabled: false
                url: 
                username: 
                password:
    data:
      mongodb:
          host: 106.12.15.6
          port: 27017
          database: admin
          username: admin
          password: 1qaz@WSX3edc
      redis:
          # 地址
          host: 106.12.15.6
          # 端口，默认为6379
          port: 16380
          # 数据库索引（因为向量检索不区分rdis数据库序号，所以只能存储在0下面）
          database: 0
          # 密码
          password: 7ujm*IK<
          # 连接超时时间
          timeout: 10s
# 日志配置
logging:
    level:
        com.ruoyi: debug
        dev.langchain4j: DEBUG
# SpringDoc OpenAPI 配置
springdoc:
    api-docs:
        enabled: true
    swagger-ui:
        enabled: true
#向量数据库配置
langchain4j:
    community:
        redis:
            # 是否启用 ，默认true
            enabled: true
            # 地址
            host: 106.12.15.6
            # 端口，默认为6379
            port: 16379
            # redis没有用户，但注册时如果带了密码就必须带用户名称，这里给默认用户
            user: default
            # 缓存前缀
            prefix: "knowledge_local:"
            # 向量数据库索引
            index-name: ai-embeddings-local
            # 密码
            password: 7ujm*IK<
            dimension: 1024  # 与 embedding-model 维度匹配 text-embedding-v3是1024
            metadata-keys:
                - $.kbId
                - $.docId
                - $.userId
                - $.tenant
                - $.index
                - $.timestamp